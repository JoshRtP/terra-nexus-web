---
// Regenerative Agriculture. Content lives in src/data/expertise/, structure in
// components/ExpertiseTopicPage.astro — this route is just the binding between
// the two.
//
// This replaces the page-local scroll-stack composition that ran here before
// (hero → overview/benefits → market validation → our approach → who we
// support, each a sticky ScrollPanel "act"). Everything in the ten-section
// template's scope is carried over; the one block with no home in the fixed
// section order is the seven-participant "Who We Support" band, whose content
// is parked in src/data/expertise/_parked-participants.ts rather than deleted.
// _ScrollPanel.astro and _original-backup.astro are left in place, unrouted.
import ExpertiseTopicPage from '../../../components/ExpertiseTopicPage.astro';
import { regenerativeAgriculture } from '../../../data/expertise/regenerative-agriculture';
---

<ExpertiseTopicPage topic={regenerativeAgriculture} />
