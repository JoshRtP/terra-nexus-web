---
interface Props {
  eyebrow?: string;
  title: string;
  lead?: string;
  primaryCta?: { label: string; href: string };
  secondaryCta?: { label: string; href: string };
}

const { eyebrow, title, lead, primaryCta, secondaryCta } = Astro.props;
---

{/*
  Representative M4 Tailwind migration (2026-08-16) — see
  docs/architecture/web-platform-architecture.md §5 and the
  terra-nexus-design-system skill. Replaces this component's scoped
  <style> block with Tailwind utilities backed by the tn- token bridge in
  src/styles/tailwind.css, proving the pattern before wider (M7) adoption.
  Visual output is intentionally unchanged from the plain-CSS version:
  same gradient background, same glow, same spacing/typography — this is
  a styling-mechanism swap, not a redesign. `.container`/`.eyebrow`/`.btn`
  classes from design-system.css are kept as-is (not yet migrated; M7
  scope) since PageHero only owns the hero-specific wrapper/spacing.
*/}
<section class="relative overflow-hidden border-b border-tn-border bg-[image:var(--gradient-surface)] pt-20 pb-16">
  <div
    class="pointer-events-none absolute -top-[30%] -right-[15%] h-[50rem] w-[50rem] bg-[radial-gradient(circle,rgba(168,208,219,0.4)_0%,transparent_65%)]"
    aria-hidden="true"
  >
  </div>
  <div class="container relative">
    {eyebrow && <span class="eyebrow mb-4">{eyebrow}</span>}
    <h1 class="mb-5 max-w-[var(--max-width-text)]">{title}</h1>
    {lead && (
      // leading-[var(--line-relaxed)], not Tailwind's built-in leading-relaxed
      // (1.625) — design-system.css's --line-relaxed is 1.7, a different
      // value; using the token keeps this pixel-identical to the pre-M4 CSS.
      <p class="max-w-[var(--max-width-text)] text-lg text-tn-text-secondary leading-[var(--line-relaxed)]">{lead}</p>
    )}
    {(primaryCta || secondaryCta) && (
      <div class="mt-8 flex flex-wrap gap-4">
        {primaryCta && <a href={primaryCta.href} class="btn btn-primary">{primaryCta.label}</a>}
        {secondaryCta && <a href={secondaryCta.href} class="btn btn-secondary">{secondaryCta.label}</a>}
      </div>
    )}
  </div>
</section>
